// we need a loop because we need to do things from a specific strat to a specific end.
// the starting point of the loop is reaching 2 miles
// when the runner reach 6 miles the loop should stop
// We specify the stop point in the loop
// the variable number that suposed to be the counter in the loop 
// variables of numbers 
for (var i = 0 ; i<=6 ; i++){
    if ((i>0) && (i%2 == 0)){
        console.log("Take Your Candy :)")
    }
    
}